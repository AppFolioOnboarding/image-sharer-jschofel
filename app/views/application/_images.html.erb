<% images.each do |image| %>
  <div class="image-tag-container">
    <%= image_tag image.url %>

    <p>
      <% image.tag_list.each do |image_tag| %>
        <%= link_to image_tag, filter_path(tag: image_tag)  %>,
      <% end %>
    </p>
  </div>
<% end %>
